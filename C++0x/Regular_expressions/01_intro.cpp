#include <iostream>
#include <cstdlib>
#include <regex>
using namespace std;

/* Anzitutto e' necessario inserire l'header <regex> per poter lavorare con
le espressioni regolari. */

int main() {
    return(EXIT_SUCCESS);
}
